import { View, Text } from 'react-native';

export default function DriverMapScreen() {
  return (
    <View style={{ flex:1, justifyContent:'center', alignItems:'center' }}>
      <Text>Driver Map (Placeholder)</Text>
    </View>
  );
}
