// Architecture Plan
// Admin App: control + dispatch + monitoring
// Driver App: GPS + accept orders + status updates
// Customer App: order + tracking + payment

export const APP_ROLES = ['admin','driver','customer'] as const;
